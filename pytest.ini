[pytest]
testpaths = tests
pythonpath = .
addopts = -m "not integration"
markers =
    integration: calls real LLM / network (requires OPENROUTER_API_KEY)
filterwarnings =
    ignore::DeprecationWarning
